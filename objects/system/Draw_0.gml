/// @description 
game.camera.draw();
//draw_clear(c_cornflower);

//shader_set(shd_base);

//var camera = camera_get_active();

//var xto = player.x;
//var yto = player.y;
//var zto = 0+cam_z_offset;

//var _r=cam_dist;
//var _i=degtorad(cam_pitch);
//var _a=degtorad(cam_dir);
//cam_x=xto+(_r*sin(_i)*cos(_a));
//cam_y=yto+(_r*sin(_i)*sin(_a));
//cam_z=(zto+(_r*cos(_i)));


////var xto = player.x;
////var yto = player.y;
////var zto = 0+cam_z_offset;
//var xfrom = cam_x;
//var yfrom = cam_y;
//var zfrom = cam_z;

//view_mat = matrix_build_lookat(xfrom, yfrom, zfrom, xto, yto, zto, 0, 0, 1);
//proj_mat = matrix_build_projection_perspective_fov(-60, -window_get_width() / window_get_height(), 1, 32000);
//camera_set_view_mat(camera, view_mat);
//camera_set_proj_mat(camera, proj_mat);
//camera_apply(camera);

//vertex_submit(model, pr_trianglelist, sprite_get_texture(spr_grid, 0));


//shader_reset();
//with(player){
//	event_perform(ev_draw,0)	
//}